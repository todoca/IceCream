import { ContainerDirective } from './container.directive';

describe('ContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
